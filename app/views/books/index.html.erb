<p id="notice"><%= notice %></p>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Price</th>
      <th>Published date</th>
      <th>Actions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.price %></td>
        <td><%= book.published_date %></td>
        <td class="actions">
          <%= link_to("Show", book_path(book), :class => 'action show') %>
          <%= link_to("Edit", edit_book_path(book), :class => 'action edit') %>
          <%= link_to("Delete", delete_book_path(book), :class => 'action delete') %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User Book', new_book_path %><br>
<%= link_to 'Book index', new_book_path %><br>
<%= link_to 'User index', new_book_path %>